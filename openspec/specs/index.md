# index 页面规范（Spec）

## 职责
- 展示模板列表（内置+自定义），支持选择、跳转创建抽签。
- 入口页，负责引导用户进入主要流程。

## 主要流程
1. 加载模板数据（云端优先，本地回退）。
2. 用户选择模板，跳转到 create 页面。
3. 支持刷新、下拉加载更多。

## 关键接口
- 模板展示与选择。
- 跳转 create 页面。

## 交互要求
- 列表友好展示，支持分页与刷新。
- 兼容无模板时的提示。
