# create 页面规范（Spec）

## 职责
- 创建抽签，配置标题、选项、名额等参数。
- 支持选择模板或自定义。

## 主要流程
1. 用户填写表单，选择/编辑模板。
2. 校验输入，调用云函数创建抽签。
3. 跳转 result 页面展示结果。

## 关键接口
- 表单校验与提交。
- 云函数 createDraw 调用。
- 跳转 result 页面。

## 交互要求
- 表单校验友好，错误提示清晰。
- 支持模板选择与自定义。
